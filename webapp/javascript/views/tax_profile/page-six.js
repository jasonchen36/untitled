(function(){

    var $ = jQuery,
        that = app.views.profile.pageSix;

    this.init = function(){
        if ($('#page-profile-6').length > 0) {
            console.log('profile 6');
        }
    };

}).apply(app.views.profile.pageSix);