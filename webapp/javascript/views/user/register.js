(function(){

    var $ = jQuery,
        that = app.templates.user;

    this.init = function(){
        if (true){
            console.log('register page');
        }
    };

}).apply(app.views.user.register);